# Used only by the Prisma CLI (`generate`, `migrate diff`). The worker runtime never
# reads this — it talks to D1 through the @prisma/adapter-d1 driver adapter.
# Value is a throwaway local sqlite path; no real database is connected.
DATABASE_URL="file:./prisma/dev.db"
